<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!--Default button-->
    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="material:RippleAssist.Feedback" Value="#1f1f1f"/>
    </Style>

    <Style TargetType="Button" x:Key="HighlightButton" BasedOn="{StaticResource MaterialDesignRaisedButton}"/>

    <Style TargetType="Button" x:Key="DialogHighlightButton" BasedOn="{StaticResource MaterialDesignFlatButton}"/>

    <Style TargetType="Button" x:Key="DialogButton" BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="Black"/>
    </Style>
</ResourceDictionary>